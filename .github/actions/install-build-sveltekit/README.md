# Install & build SvelteKit

This composite action installs SvelteKit dependencies, installs Prisma's dependencies, generates the SvelteKit build, and updates the package.json files with the app version.
