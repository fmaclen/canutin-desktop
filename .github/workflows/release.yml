name: Build & release

on:
  push:
    branches: [main, master]

jobs:
  Release:
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]

    name: Release (${{ matrix.os }})
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3

      - name: Install SvelteKit dependencies
        working-directory: ./sveltekit
        run: npm ci

      - name: Install Prisma's dependencies
        working-directory: ./sveltekit
        run: npx prisma generate

      - name: Install Electron dependencies
        run: npm ci

      - name: Run semantic release
        run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Prepare package.json for release
        run: npm run package:github

      - name: Build & release Electron app
        uses: samuelmeuli/action-electron-builder@v1.6.0
        with:
          github_token: ${{ secrets.github_token }}
          release: ${{ startsWith(github.ref, 'refs/tags/v') }}
          build_script_name: "build:all"

      - name: "Get tagged version from commit"
        id: previoustag
        uses: "WyriHaximus/github-action-get-previous-tag@v1"

      # Only crate a release for the Linux server on Ubuntu and when a tag is present
      - name: Package Linux server
        if: matrix.os == 'ubuntu-latest' && steps.previoustag.outputs.tag
        run: ./scripts/sveltekit-package-server.sh
        env:
          APP_VERSION: ${{ steps.previoustag.outputs.tag }}

      - name: Release Linux server
        if: matrix.os == 'ubuntu-latest' && steps.previoustag.outputs.tag
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.previoustag.outputs.tag }}
          files: ./dist/canutin-server_*.tar.gz
